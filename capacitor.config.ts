import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MidtermExamLab',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
